# SFire Admin Frontend

### 介绍 📖

SFire Admin 是一款基于 Vue3.4、TypeScript、Vite5、Pinia、Element-Plus 开发的智能体管理后台系统。项目基于 Geeker-Admin 模板进行定制开发，专为 AI 智能体配置和管理场景设计，提供完善的权限管理、用户管理、财务管理和内容管理等功能。

### 技术栈 🔧

- **Vue 3.4** - 渐进式 JavaScript 框架，使用 Composition API 和 `<script setup>` 语法
- **TypeScript** - JavaScript 的超集，提供类型安全
- **Vite 5** - 下一代前端构建工具，极速的开发体验
- **Element Plus 2.7** - 基于 Vue 3 的组件库
- **Pinia 2.1** - Vue 官方推荐的状态管理库，替代 Vuex
- **Vue Router 4.4** - 官方路由管理器，支持动态路由和权限拦截
- **Axios** - 基于 Promise 的 HTTP 客户端，完整的请求拦截封装
- **ECharts 5.5** - 强大的数据可视化图表库
- **Vue I18n** - 国际化解决方案
- **WangEditor** - 富文本编辑器
- **其他工具库**：dayjs、md5、nprogress、screenfull、sortablejs 等

### 项目功能 ✨

#### 核心功能模块

1. **智能体管理**
   - 智能体的增删改查
   - 智能体配置（提示词、模型选择、参数设置）
   - 智能体预览和测试（Playground）
   - 智能体状态管理（上架/下架）

2. **用户管理**
   - 小程序用户管理
   - 管理员账号管理
   - 用户等级管理（普通用户、VIP、合伙人等）
   - 用户权限控制

3. **财务管理**
   - 算力消费日志记录
   - 算力流水查询和统计
   - 财务数据分析

4. **内容管理**
   - 首页配置管理
   - 轮播图（Banner）管理
   - 小程序配置管理

5. **系统管理**
   - 菜单管理（动态路由配置）
   - 角色管理
   - LLM 模型管理
   - 系统日志

6. **数据可视化**
   - 仪表盘数据展示
   - 数据大屏
   - 各类图表展示（折线图、柱状图、饼图、雷达图等）

7. **AI 对话功能**
   - 集成 AI 对话接口
   - 实时流式响应（SSE）

#### 技术特性

- ✅ 使用 Vue3.4 + TypeScript 开发，单文件组件 `<script setup>`
- ✅ 采用 Vite5 作为项目开发、打包工具（配置 gzip/brotli 打包、TSX 语法、跨域代理）
- ✅ 使用 Pinia 替代 Vuex，轻量、简单、易用，集成 Pinia 持久化插件
- ✅ 使用 TypeScript 对 Axios 整个二次封装（请求拦截、取消、常用请求封装）
- ✅ 基于 Element 二次封装 ProTable 组件，表格页面全部为配置项 Columns
- ✅ 支持 Element 组件大小切换、多主题布局、暗黑模式、i18n 国际化
- ✅ 使用 VueRouter 配置动态路由权限拦截、路由懒加载，支持页面按钮权限控制
- ✅ 使用 KeepAlive 对页面进行缓存，支持多级嵌套路由缓存
- ✅ 常用自定义指令开发（权限、复制、水印、拖拽、节流、防抖、长按）
- ✅ 使用 Prettier 统一格式化代码，集成 ESLint、Stylelint 代码校验规范
- ✅ 使用 husky、lint-staged、commitlint、czg、cz-git 规范提交信息

### 快速开始 🚀

#### 环境要求

- Node.js >= 16.18.0
- pnpm >= 7.0.0（推荐）或 npm/yarn

#### 安装步骤

1. **克隆项目**

```bash
git clone <repository-url>
cd admin-sfire-ai/frontend
```

2. **安装依赖**

```bash
# 使用 pnpm（推荐）
pnpm install

# 或使用 npm
npm install

# 或使用 yarn
yarn install
```

3. **配置环境变量**

复制并配置环境变量文件：

```bash
# .env.development - 开发环境
# .env.test - 测试环境
# .env.production - 生产环境
```

主要配置项：
- `VITE_PORT` - 开发服务器端口
- `VITE_PUBLIC_PATH` - 公共基础路径
- `VITE_PROXY` - API 代理配置
- `VITE_DROP_CONSOLE` - 是否移除 console

4. **启动开发服务器**

```bash
pnpm dev
# 或
pnpm serve
```

访问地址：http://localhost:5173（默认端口）

5. **构建生产版本**

```bash
# 开发环境构建
pnpm build:dev

# 测试环境构建
pnpm build:test

# 生产环境构建
pnpm build:pro
```

6. **预览构建结果**

```bash
pnpm preview
```

### 代码规范 🔍

#### Lint 检查

```bash
# ESLint 检测代码
pnpm lint:eslint

# Prettier 格式化代码
pnpm lint:prettier

# Stylelint 格式化样式
pnpm lint:stylelint

# 执行所有 lint（提交前自动执行）
pnpm lint:lint-staged
```

#### TypeScript 类型检查

```bash
pnpm type:check
```

#### 提交代码

```bash
# 提交代码（提交前会自动执行 lint:lint-staged 命令）
pnpm commit
```

提交规范使用 [Conventional Commits](https://www.conventionalcommits.org/)，通过 cz-git 工具进行交互式提交。

### 项目结构 📁

```
frontend/
├── .husky                  # Git hooks 配置
├── .vscode                 # VSCode 推荐配置
├── build                   # Vite 配置项
│   ├── getEnv.ts          # 环境变量处理
│   ├── plugins.ts         # Vite 插件配置
│   └── proxy.ts           # 代理配置
├── public                  # 静态资源文件（该文件夹不会被打包）
├── src
│   ├── api                 # API 接口管理
│   │   ├── config          # API 配置文件
│   │   ├── helper          # API 辅助函数
│   │   ├── interface       # API 类型定义
│   │   └── modules         # 各模块 API
│   │       ├── agent.ts    # 智能体相关 API
│   │       ├── user.ts     # 用户相关 API
│   │       ├── ai.ts       # AI 对话 API
│   │       ├── banner.ts   # 轮播图 API
│   │       ├── dashboard.ts # 仪表盘 API
│   │       ├── llmModel.ts # LLM 模型 API
│   │       └── ...
│   ├── assets              # 静态资源文件
│   │   ├── fonts           # 字体文件
│   │   ├── iconfont        # 图标字体
│   │   ├── icons           # SVG 图标
│   │   ├── images          # 图片资源
│   │   └── json            # JSON 数据
│   ├── components          # 全局组件
│   │   ├── ECharts         # ECharts 图表组件
│   │   ├── ProTable        # 高级表格组件
│   │   ├── SearchForm      # 搜索表单组件
│   │   ├── Upload          # 文件上传组件
│   │   ├── WangEditor      # 富文本编辑器
│   │   └── ...
│   ├── config              # 全局配置项
│   ├── directives          # 全局指令
│   │   └── modules         # 指令模块
│   │       ├── auth.ts     # 权限指令
│   │       ├── copy.ts     # 复制指令
│   │       ├── debounce.ts # 防抖指令
│   │       ├── throttle.ts # 节流指令
│   │       └── ...
│   ├── enums               # 项目常用枚举
│   ├── hooks               # 常用 Hooks 封装
│   │   ├── useAuthButtons.ts    # 权限按钮 Hook
│   │   ├── useChat.ts           # 聊天 Hook
│   │   ├── useCompute.ts        # 算力相关 Hook
│   │   ├── useSSE.ts            # SSE 流式响应 Hook
│   │   └── ...
│   ├── languages           # 语言国际化 i18n
│   ├── layouts             # 框架布局模块
│   │   ├── components      # 布局组件
│   │   ├── LayoutClassic   # 经典布局
│   │   ├── LayoutColumns   # 分栏布局
│   │   ├── LayoutTransverse # 横向布局
│   │   └── LayoutVertical  # 纵向布局
│   ├── routers             # 路由管理
│   │   └── modules         # 路由模块
│   ├── stores              # Pinia Store
│   │   └── modules         # Store 模块
│   ├── styles              # 全局样式文件
│   │   ├── var.scss        # SCSS 变量
│   │   ├── common.scss     # 公共样式
│   │   └── theme           # 主题配置
│   ├── typings             # 全局 TypeScript 声明
│   ├── utils               # 常用工具库
│   ├── views               # 项目所有页面
│   │   ├── agent           # 智能体管理
│   │   │   ├── components  # 智能体组件
│   │   │   │   ├── AgentForm.vue      # 智能体表单
│   │   │   │   └── Playground         # 预览测试
│   │   │   │       ├── ChatPanel.vue  # 聊天面板
│   │   │   │       ├── ConfigPanel.vue # 配置面板
│   │   │   │       └── PromptEditor.vue # 提示词编辑器
│   │   │   └── index.vue   # 智能体列表
│   │   ├── miniprogram     # 小程序管理
│   │   │   ├── banner      # 轮播图管理
│   │   │   ├── user        # 用户管理
│   │   │   └── config      # 配置管理
│   │   ├── system          # 系统管理
│   │   │   ├── agentManage # 智能体配置
│   │   │   ├── userManage  # 用户管理
│   │   │   ├── roleManage  # 角色管理
│   │   │   ├── menuMange   # 菜单管理
│   │   │   └── llmModelManage # LLM 模型管理
│   │   ├── dashboard       # 仪表盘
│   │   ├── home            # 首页
│   │   └── ...
│   ├── App.vue             # 项目主组件
│   ├── main.ts             # 项目入口文件
│   └── vite-env.d.ts       # Vite 类型声明
├── .editorconfig           # 编辑器配置
├── .env.development        # 开发环境配置
├── .env.production         # 生产环境配置
├── .env.test               # 测试环境配置
├── .eslintrc.cjs           # ESLint 配置
├── .prettierrc.cjs         # Prettier 配置
├── .stylelintrc.cjs        # Stylelint 配置
├── commitlint.config.cjs   # Commitlint 配置
├── lint-staged.config.cjs  # Lint-staged 配置
├── index.html              # 入口 HTML
├── package.json            # 依赖包管理
├── postcss.config.cjs      # PostCSS 配置
├── tsconfig.json           # TypeScript 配置
└── vite.config.ts          # Vite 配置文件
```

### API 对接 🔌

前端通过 Axios 与后端 API 进行交互，API 基础配置在 `src/api/config` 中。

#### 后端 API 地址

开发环境通过 Vite 代理转发到后端服务，生产环境需要配置实际的 API 地址。

#### API 模块

- `agent.ts` - 智能体管理接口
- `user.ts` - 用户管理接口
- `ai.ts` - AI 对话接口
- `banner.ts` - 轮播图接口
- `dashboard.ts` - 仪表盘数据接口
- `llmModel.ts` - LLM 模型接口
- `menu.ts` - 菜单接口
- `role.ts` - 角色权限接口
- `homeConfig.ts` - 首页配置接口
- `adminUser.ts` - 管理员用户接口

#### 响应格式

后端统一返回格式：

```typescript
{
  code: number;    // 状态码：200 成功，其他为失败
  data: any;       // 返回数据
  msg: string;     // 提示信息
}
```

### 权限管理 🔐

系统支持基于角色的权限控制（RBAC）：

1. **路由权限** - 通过动态路由实现菜单权限控制
2. **按钮权限** - 通过 `v-auth` 指令控制按钮显示/隐藏
3. **API 权限** - 后端接口层面进行权限验证

权限相关文件：
- `src/directives/modules/auth.ts` - 权限指令
- `src/hooks/useAuthButtons.ts` - 权限按钮 Hook
- `src/stores/modules/auth.ts` - 权限状态管理

### 开发指南 📝

#### 添加新页面

1. 在 `src/views` 下创建页面组件
2. 在路由配置中添加路由（或通过菜单管理配置）
3. 如需 API 调用，在 `src/api/modules` 下添加对应接口

#### 添加新组件

1. 全局组件放在 `src/components` 目录
2. 页面级组件放在对应 `views` 目录下的 `components` 文件夹

#### 使用 ProTable 组件

ProTable 是基于 Element Plus Table 的高级封装，支持配置化开发：

```vue
<template>
  <ProTable ref="proTable" title="用户列表" :columns="columns" :request-api="getUserList">
    <!-- 表格 header 按钮 -->
    <template #tableHeader="scope">
      <el-button type="primary" :icon="Plus" @click="handleAdd">新增用户</el-button>
    </template>
  </ProTable>
</template>
```

#### 自定义 Hook

项目中封装了多个常用 Hook：

- `useTable` - 表格数据管理
- `useAuthButtons` - 权限按钮控制
- `useChat` - 聊天功能
- `useSSE` - SSE 流式响应
- `useCompute` - 算力相关功能

### 浏览器支持 🌐

- 本地开发推荐使用 Chrome 最新版浏览器
- 生产环境支持现代浏览器，不再支持 IE 浏览器

| ![Edge](https://img.shields.io/badge/Edge-0078D4?logo=microsoft-edge&logoColor=white) | ![Firefox](https://img.shields.io/badge/Firefox-FF7139?logo=Firefox-Browser&logoColor=white) | ![Chrome](https://img.shields.io/badge/Chrome-4285F4?logo=GoogleChrome&logoColor=white) | ![Safari](https://img.shields.io/badge/Safari-000000?logo=Safari&logoColor=white) |
| :-------------------------------------------------------------------------------------: | :--------------------------------------------------------------------------------------------: | :---------------------------------------------------------------------------------------: | :----------------------------------------------------------------------------------: |
|                                           最新 2 个版本                                            |                                                     最新 2 个版本                                                      |                                                  最新 2 个版本                                                  |                                                最新 2 个版本                                                |

### 性能优化 ⚡

- 路由懒加载，减少首屏加载时间
- 组件按需引入，减少打包体积
- 使用 KeepAlive 缓存页面状态
- Gzip/Brotli 压缩，减少资源大小
- 代码分割，优化打包体积
- 图片资源优化和懒加载

### 常见问题 ❓

#### 1. 安装依赖失败

- 检查 Node.js 版本是否符合要求（>= 16.18.0）
- 清除缓存后重新安装：`pnpm store prune && pnpm install`
- 使用国内镜像：配置 `.npmrc` 或使用 `pnpm config set registry`

#### 2. 开发服务器启动失败

- 检查端口是否被占用
- 检查环境变量配置是否正确
- 查看控制台错误信息

#### 3. 接口请求失败

- 检查后端服务是否启动
- 检查代理配置是否正确
- 检查请求地址和参数

### 更新日志 📋

详细更新日志请查看 [CHANGELOG.md](./CHANGELOG.md)

### 许可证 📄

本项目采用 [MIT](./LICENSE) 许可证

### 联系方式 📧

- 项目作者：SFire
- 邮箱：848130454@qq.com
- GitHub：https://github.com/HalseySpicy

---

**注意**：本项目基于 Geeker-Admin 模板进行定制开发，感谢 Geeker-Admin 项目提供的优秀基础框架。
